{
  "name": "Example: data-driven matrix",
  "label": "example/matrix",
  "description": "Runs the same steps once per matrix row, with {{heading}} bound to each row's value. Hermetic (data: URL, no server).",
  "matrix": [
    {"heading": "Alpha"},
    {"heading": "Beta"}
  ],
  "steps": [
    {"action": "open", "url": "data:text/html,<title>WAT</title><h1 id='t'>{{heading}}</h1>"},
    {"action": "assert_element_text", "selector": "#t", "value": "{{heading}}"}
  ]
}

{
  "name": "Example: save a session for reuse via storage_state",
  "label": "example/storage",
  "description": "Sets a cookie then persists cookies + localStorage with save_storage_state. Point other flows at the file via config storage_state to skip logging in. Hermetic (no server).",
  "steps": [
    {"action": "open", "url": "data:text/html,<title>WAT</title><p>session demo</p>"},
    {"action": "set_cookie", "name": "sid", "value": "demo-123", "comment": "scoped to base_url"},
    {"action": "save_storage_state", "path": "artifacts/example.storage-state.json",
     "comment": "gitignored; restore elsewhere with storage_state = \"artifacts/example.storage-state.json\""},
    {"action": "assert_title", "value": "WAT"}
  ]
}
